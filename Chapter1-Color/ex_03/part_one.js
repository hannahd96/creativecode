//  throws more errors and disables some features in an effort to make code more readable and accurate
'use strict';

// global variables
var segmentCount = 360;
var radius = 300;

// sets up canvas width + height
// no outline on canvas
function setup() {
  createCanvas(800, 800);
  noStroke();
}
